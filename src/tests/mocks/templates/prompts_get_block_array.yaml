prompts:
  - $cndi.get_block(https://raw.githubusercontent.com/polyseam/common-blocks/main/common/core-prompts.yaml):
      {}

outputs:
  cndi_config:
    cndi_version: v2
  readme:
    heading: "# hello world"
  env:
    FOO: bar
