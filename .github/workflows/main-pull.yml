on:
  pull_request:
    branches:    
      - main
jobs:
  test_and_lint: # cndi
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
        
      - name: use deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.44
      
      - name: Check formatting
        run: deno fmt --check
        
      - name: Lint
        run: deno lint

      - name: Test
        run: deno task test
