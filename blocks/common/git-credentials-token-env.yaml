$cndi.comment(git_credentials): Git Credentials
GIT_REPO: "{{ $cndi.get_prompt_response(git_repo) }}"
GIT_USERNAME: "{{ $cndi.get_prompt_response(git_username) }}"
GIT_TOKEN: "{{ $cndi.get_prompt_response(git_token) }}"