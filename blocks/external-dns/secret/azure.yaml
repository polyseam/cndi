$cndi.comment(external-dns-secret): External DNS Credentials
apiVersion: v1
kind: Secret
metadata:
  name: external-dns
  namespace: external-dns
stringData:
  azure.json: $cndi_on_ow.seal_secret_from_env_var(EXTERNAL_DNS_AZURE_CREDENTIALS)
