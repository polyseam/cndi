$cndi.comment(azurejson): ExternalDNS AWS Credentials
EXTERNAL_DNS_AWS_ACCESS_KEY_ID: "{{ $cndi.get_prompt_response(aws_access_key_id) }}"
EXTERNAL_DNS_AWS_SECRET_ACCESS_KEY: "{{ $cndi.get_prompt_response(aws_secret_access_key) }}"